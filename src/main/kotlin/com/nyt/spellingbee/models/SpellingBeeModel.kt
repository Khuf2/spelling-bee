package com.nyt.spellingbee.models

class SpellingBeeModel {
    /*
     * This model will include:
     * * found words (List<String>)
     * * letters (List<String>)
     * * prefixes (Map<String, Int>)
     * * core letter (String)
     * * summary (Map<String, Int>)
     * * Matrix
     * * Points
     */

    /*
     * It needs to be Immutable and have:
     * * A builder function
     * * A load from file option for saves
     */
}